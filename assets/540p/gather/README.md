# Gather Templates

Upload 540p template images used by Auto-Gather.

Required generic templates:
- gather_btn.png
- march_confirm_btn.png

Optional helper template:
- march_queue_icon.png

Resource tile templates (per resource type + level) are configured in the Resource Tile Template Configuration dialog and can use any filename/path.
